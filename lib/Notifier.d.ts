import React from "react";
import { NotifierProps } from "./types";
export declare const Notifier: ({ visible, onClose, title, message, icon, iconColor, primaryButton, secondaryButton, animationDuration, backdropColor, iconBackGroundColor, theme, }: NotifierProps) => React.JSX.Element | null;
export default Notifier;
